# 更新记录

本文件记录文档、工程骨架和后续功能变更。每次有意义工作结束后都要更新。

## 2026-05-02 中文化文档结构迁移

### 改动内容

- 将长期文档入口整理为中文命名结构。
- 保留 `README.md` 作为 GitHub 项目首页。
- 保留 `AGENTS.md` 作为 Codex/AI agent 长期规则。
- 保留 `.env.example` 作为环境变量样例。
- 新增 `计划模板.md`、`docs/架构说明.md`、`docs/接口文档.md`、`docs/安全要求.md`、`docs/分支与回滚.md`。
- 将各子目录 `README.md` 迁移为 `说明.md`。
- 删除旧中文规划入口，避免两套文档重复维护。

### 当前状态

- 当前仍是工程骨架阶段。
- 尚未安装依赖。
- 尚未初始化 Taro、NestJS、Next.js、Prisma 或微信开发者工具。
- 尚未写页面、组件、接口、数据库模型或业务函数。

### 下一步

- Owner 审阅文档结构。
- 如果批准进入可运行开发阶段，建议先建立工程地基：pnpm workspace 脚本、strict TypeScript、lint、typecheck、test、build 和 CI。

### 风险

- 根目录存在 `project.config.json`，其中包含微信 `appid`，后续需要确认它是测试配置、正式配置还是应迁移为样例配置。
- 真实医院电话、地址、接诊状态和地图 Key 仍未配置，后续不得使用未经验证的数据上线。
- 红旗规则正式实现前仍需兽医顾问审核。

### 涉及文件

- `README.md`
- `AGENTS.md`
- `计划模板.md`
- `更新记录.md`
- `docs/架构说明.md`
- `docs/接口文档.md`
- `docs/安全要求.md`
- `docs/分支与回滚.md`
- `apps/**/说明.md`
- `services/**/说明.md`
- `packages/**/说明.md`

## 2026-05-01 工程骨架阶段初始化

### 改动内容

- 创建 TypeScript monorepo 骨架：`apps/`、`services/`、`packages/`。
- 创建小程序优先的用户端骨架 `apps/miniapp/`。
- 创建医院后台、运营后台、后端 API、共享类型、API Client、安全规则和共享配置的占位目录。
- 新增 `package.json`、`pnpm-workspace.yaml`、`.env.example`、`.github/CODEOWNERS` 和 PR 模板。
- 明确项目已进入工程骨架阶段，但尚不进入业务代码实现阶段。

### 当前状态

- 当前只有骨架、说明文档和配置占位。
- 尚未写页面、组件、接口、数据库模型或业务函数。
- 尚未安装依赖。
- 尚未初始化微信开发者工具、Taro、NestJS、Next.js 或 Prisma。
- MVP 仍坚持微信小程序优先。

### 下一步

- Owner 审阅骨架目录和文档边界。
- 若确认进入可运行开发阶段，下一步建议先初始化 `apps/miniapp` 的 Taro + React + TypeScript 工程。
- 初始化前需要再次确认依赖安装、Node/pnpm 版本和首批真实医院数据策略。

### 风险

- 当前骨架不可运行，这是刻意控制范围的结果。
- 真实医院电话、地址、接诊状态和地图 Key 仍未配置。
- 红旗规则正式实现前仍需兽医顾问审核。

## 2026-05-01 Git 分支与回滚规则补充

### 改动内容

- 明确 `main` 保持稳定。
- 大型工程实现、脚手架搭建、依赖安装和大改文档前必须先开 feature 分支。
- 记录常用回滚方式：删除未合并 feature 分支、`git restore`、`git clean`、`git revert`。

### 风险

- `git clean -fd` 会删除未跟踪文件，执行前必须确认这些文件不需要保留。

## 2026-05-01 绑定 GitHub 远程仓库

### 改动内容

- 将本地仓库绑定到 GitHub 远程仓库：`https://github.com/puY123610/-sos.git`。
- 拉取远程 `main` 后合并历史，保留本地完整项目说明。

### 风险

- 远程仓库名为 `-sos`，可以使用，但命名较特殊，后续可在 GitHub 设置中改名。

## 2026-05-01 Git 仓库初始化与首个提交

### 改动内容

- 新增 `.gitignore`。
- 新增项目说明。
- 初始化本地 git 仓库，默认分支为 `main`。
- 创建首个本地提交：`docs: initialize pet emergency planning repository`。

## 2026-05-01 文档阶段项目骨架规划

### 改动内容

- 明确当前阶段只做规划文档。
- 明确 MVP 首发微信小程序、SOS 直连医院方向和医疗安全边界。
- 规划未来 TypeScript monorepo 骨架、用户端、医院端、后端模块、数据对象和扩展点。

## 2026-04-30 项目计划书交付

### 改动内容

- 基于 `/Users/jack/Desktop/pet.md` 和项目规则生成项目计划书。
- 计划书覆盖项目定位、MVP 范围、医疗安全边界、AI 治理、技术架构、Sprint 计划、团队分工、验收治理、风险评估和近期行动清单。

### 风险

- 红旗急症规则在正式上线前仍需兽医顾问审核。
- 医院电话、地址和可接诊状态不能直接承诺准确，需要标记数据来源并逐步人工校验。
