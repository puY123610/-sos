# 小程序源码目录

未来 Taro 源码根目录占位。

当前只保留目录说明，不写页面、组件、路由或业务逻辑。

## 规划子目录

- `app-shell/`：应用壳、权限入口、全局状态边界。
- `pages/`：用户可见页面。
- `components/`：小程序端组件。
- `services/`：调用 API Client 的小程序端封装。
- `platform/`：微信能力和未来移动端能力适配层。
- `styles/`：样式和主题占位。
