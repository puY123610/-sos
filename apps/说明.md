# 应用目录

前端应用入口目录。

当前只创建骨架，不包含可运行页面代码。

## 目录

- `miniapp/`：MVP 首发用户端，微信小程序优先，后续可扩展 H5 或 App。
- `hospital-admin/`：医院后台，占位目录，后续接收 SOS 队列和回拨处理。
- `ops-admin/`：运营后台，占位目录，后续管理医院资料和数据质检。

## 边界

- MVP 不引入原生 iOS 或 Android 工程。
- 用户端不展示红黄绿风险等级。
- 所有医疗安全规则必须由后端和文档显式维护。
